---
title: "Sentiment Analysis Report"
author: "Colombini Francesco & Hosen Jabed"
date: "sys.Date()"
output: 
  pdf_document:
    latex_engine: xelatex
knitr:
  opts_chunk:
    echo: false
    
editor_options: 
  markdown: 
    wrap: 72
---

```{r setup, include=FALSE}
# Setup environment
knitr::opts_chunk$set(
	echo = TRUE,
	warning = FALSE,
	message = FALSE
)

# Use project root for relative paths and allow a private local data folder.
knitr::opts_knit$set(root.dir = normalizePath(".", 
						winslash = "/", mustWork = TRUE))
private_data_dir <- Sys.getenv("PRIVATE_DATA_DIR", unset = "data")
```

## Test jsonlite

```{r, EDA}
library(tidyverse)
SA <- as_tibble(read_csv2("sentiment_analysis_test.csv"))
summary(SA)
SA_train <- as_tibble(read_csv2("sentiment_analysis_train.csv"))
summary(SA_train)
```
